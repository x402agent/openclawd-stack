# ── Required ──────────────────────────────────────────────────────────────────
# Create a bot via @BotFather on Telegram and paste the token here
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Solana RPC endpoint (mainnet-beta)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# ── Optional ──────────────────────────────────────────────────────────────────
# WebSocket URL for real-time monitoring (derived from RPC if not set)
# Recommended: use a paid RPC (Helius, QuickNode, Triton) for reliable WS
# SOLANA_WS_URL=wss://atlas-mainnet.helius-rpc.com/?api-key=YOUR_KEY

# Restrict bot to specific Telegram user IDs (comma-separated)
# ALLOWED_USER_IDS=123456789,987654321

# Polling interval in seconds (used when WebSocket is unavailable)
# POLL_INTERVAL_SECONDS=15

# Log level: debug | info | warn | error
# LOG_LEVEL=info

# ── Token Launch Monitor ──────────────────────────────────────────────────────
# Enable real-time monitoring of new PumpFun token launches
# ENABLE_LAUNCH_MONITOR=true

# Only notify for tokens that have a GitHub link in their metadata
# GITHUB_ONLY_FILTER=false

# IPFS gateway for fetching token metadata (must end with /)
# IPFS_GATEWAY=https://cf-ipfs.com/ipfs/

# ── REST API Server ──────────────────────────────────────────────────────────
# Enable the scalable REST API alongside the Telegram bot
# ENABLE_API=true

# Port for the HTTP API server
# API_PORT=3000

# API keys for authentication (comma-separated). If empty, no auth is required.
# API_KEYS=sk_live_abc123,sk_live_def456

# CORS allowed origins (* = all)
# CORS_ORIGINS=*

# Max watches per API client
# MAX_WATCHES_PER_CLIENT=100

# Rate limiting: max requests per window
# RATE_LIMIT_MAX=100

# Rate limiting: window duration in milliseconds (default: 60s)
# RATE_LIMIT_WINDOW_MS=60000

# Max claim events buffered in memory for GET /claims (older evicted)
# CLAIM_BUFFER_SIZE=10000

# HMAC-SHA256 secret for signing webhook payloads (receivers verify via X-PumpFun-Signature)
# WEBHOOK_SECRET=whsec_your_secret_here

# ── Event Monitor (graduation, whale trades, fee distributions) ───────────────
# Enable graduation/AMM migration alerts (default: true)
# ENABLE_GRADUATION_ALERTS=true

# Enable whale trade alerts (default: false)
# ENABLE_TRADE_ALERTS=false

# Minimum SOL amount for a trade to trigger a whale alert (default: 10)
# WHALE_THRESHOLD_SOL=10

# Enable creator fee distribution alerts (default: false)
# ENABLE_FEE_DISTRIBUTION_ALERTS=false

